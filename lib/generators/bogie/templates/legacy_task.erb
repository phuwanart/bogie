require 'bogie'
include Bogie

namespace :db do
  namespace :migrate do
  
    desc 'Migrates <%= file_name.pluralize.downcase %>'
    task :<%= file_name.pluralize.downcase %> => :environment do
      Bogie.run :<%= file_name.pluralize.downcase %>
    end

  end
end