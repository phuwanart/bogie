class Legacy<%= file_name.squish.gsub(' ', '_').gsub('-','_').singularize.classify %> < LegacyBase
  self.table_name = "legacy_<%= file_name.squish.gsub(' ', '_').gsub('-','_').tableize %>"
  # self.primary_key = "old_id"
  
  # To use autogenerated ids uncomment below
  def dont_migrate_ids
    false
  end

  def migrate_where
    {id: self.id}
  end

  def map
    {
      field_one: self.old_field_one.squish,
      field_two: self.old_field_two.squish
    }
  end

  def associate
    {
      # association: records.to.associate
    }
  end

end
